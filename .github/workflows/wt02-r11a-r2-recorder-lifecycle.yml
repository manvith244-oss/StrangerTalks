name: WT02 R11A-R4 Observation Truth RED

on:
  push:
    branches:
      - wind-tunnel/wt02-r11a-r4-observation-truth
    paths:
      - test/support/wt02_context_recorder.ex
      - test/support/wt02_context_recorder_lifecycle_harness.exs
      - test/support/wt02_context_recorder_target_gate.ex
      - .github/workflows/wt02-r11a-r2-recorder-lifecycle.yml

permissions:
  contents: read

jobs:
  synthetic-observation-truth:
    runs-on: ubuntu-latest
    env:
      MIX_ENV: test
    steps:
      - name: Checkout exact diagnostic branch generation
        uses: actions/checkout@v4

      - name: Record identity
        shell: bash
        run: |
          set -euo pipefail
          echo "HEAD=$(git rev-parse HEAD)"
          echo "TREE=$(git rev-parse HEAD^{tree})"
          git status --short

      - name: Set up BEAM
        uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.18.4"
          otp-version: "27.3.4"

      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get

      - name: Compile diagnostic generation
        run: mix compile

      - name: Run synthetic observation-truth RED harness only
        run: mix run --no-start test/support/wt02_context_recorder_lifecycle_harness.exs

      - name: Diff hygiene
        if: always()
        shell: bash
        run: |
          git diff --check
          git status --short
