name: T02 Causal Trace Bootstrap

on:
  push:
    branches:
      - team2/runtime-restart-reconciliation-fix-lab
    paths:
      - .github/workflows/t02-runtime-generation-causal-trace.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap:
    runs-on: ubuntu-latest
    steps:
      - name: Prove workflow registration
        run: echo T02_CAUSAL_TRACE_BOOTSTRAP=PASS
