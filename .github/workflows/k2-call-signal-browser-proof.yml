name: K2 Call Signal Browser Proof

on:
  push:
    branches:
      - verify/composed-call-signal-fix-2026-08-29
    paths:
      - .github/workflows/k2-call-signal-browser-proof.yml

permissions:
  contents: read

env:
  MIX_ENV: test
  STRANGERTALKS_LOCAL_DB_USER: strangertalks_local
  STRANGERTALKS_LOCAL_DB_PASSWORD: strangertalks_test
  PORT: "4000"
  STRANGERTALKS_BROWSER_BASE_URL: http://localhost:4000
  SOURCE_SHA: 2c53f29dbde5d7848b42bfe3d3c44054d01f153f

jobs:
  browser-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: strangertalks_local
          POSTGRES_PASSWORD: strangertalks_test
          POSTGRES_DB: strangertalks_new_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U strangertalks_local -d strangertalks_new_test"
          --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ env.SOURCE_SHA }}
          fetch-depth: 0
      - name: Exact source proof
        run: test "$(git rev-parse HEAD)" = "$SOURCE_SHA"
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.18.4"
          otp-version: "27.3.4"
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Install dependencies
        run: |
          mix local.hex --force
          mix local.rebar --force
          mix deps.get
          npm ci
          npx playwright install --with-deps chromium
      - name: Unit regression proof
        run: |
          node --test test/js/live_call_signal_queue_regression_test.mjs
          node --test test/js/live_call_test.mjs
      - name: Add logging-only call-state instrumentation
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          app_path = Path("priv/static/assets/app.js")
          app = app_path.read_text()
          needle = "function renderLiveCallState(state) {\n"
          assert app.count(needle) == 1, app.count(needle)
          replacement = '''function renderLiveCallState(state) {
            console.log(`[K2_CALL_STATE] ${JSON.stringify({
              attempt: state?.callAttemptId || null,
              status: state?.status || null,
              selfVideo: Boolean(state?.selfVideo),
              peerVideo: Boolean(state?.peerVideo),
              mediaGeneration: Number.isInteger(state?.mediaGeneration) ? state.mediaGeneration : null
            })}`)
          '''
          app_path.write_text(app.replace(needle, replacement, 1))

          test_path = Path("test/js/browser_e2e_test.mjs")
          test = test_path.read_text()
          old = '''  page.on("console", message => {
              if (message.type() === "error") consoleErrors.push(message.text())
            })
          '''
          new = '''  page.on("console", message => {
              const text = message.text()
              if (text.startsWith("[K2_CALL_STATE]")) console.log(text)
              if (message.type() === "error") consoleErrors.push(text)
            })
          '''
          assert test.count(old) == 1, test.count(old)
          test_path.write_text(test.replace(old, new, 1))
          PY
      - name: Prepare isolated browser database
        run: |
          mix ecto.drop --quiet || true
          mix ecto.create --quiet
          mix ecto.migrate --quiet
      - name: Start isolated Phoenix runtime
        shell: bash
        run: |
          set -euo pipefail
          mix run --no-start -e '
            repo = Application.fetch_env!(:strangertalks_new, StrangertalksNew.Repo)
            repo = repo |> Keyword.put(:pool, DBConnection.ConnectionPool) |> Keyword.put(:pool_size, 24)
            Application.put_env(:strangertalks_new, StrangertalksNew.Repo, repo)
            config = Application.fetch_env!(:strangertalks_new, StrangertalksNewWeb.Endpoint)
            Application.put_env(:strangertalks_new, StrangertalksNewWeb.Endpoint, Keyword.put(config, :server, true))
            {:ok, _} = Application.ensure_all_started(:strangertalks_new)
            Process.sleep(:infinity)
          ' > /tmp/k2-call-proof-phoenix.log 2>&1 &
          echo $! > /tmp/k2-call-proof-phoenix.pid
          for attempt in $(seq 1 60); do
            if curl -fsS "$STRANGERTALKS_BROWSER_BASE_URL/health/live" >/dev/null 2>&1 || curl -fsS "$STRANGERTALKS_BROWSER_BASE_URL/" >/dev/null 2>&1; then exit 0; fi
            if ! kill -0 "$(cat /tmp/k2-call-proof-phoenix.pid)" 2>/dev/null; then cat /tmp/k2-call-proof-phoenix.log; exit 1; fi
            sleep 1
          done
          cat /tmp/k2-call-proof-phoenix.log
          exit 1
      - name: Run unmodified #85 #86 #87 assertions with state logging
        shell: bash
        run: |
          set -o pipefail
          node --test --test-concurrency=1 --test-name-pattern='Feature 1Q E2E-(RTV|REACTION|RING)' test/js/browser_e2e_test.mjs 2>&1 | tee /tmp/k2-call-proof.tap
      - name: Build proof report
        if: always()
        shell: bash
        run: |
          {
            echo "SOURCE_SHA=$SOURCE_SHA"
            echo "--- CALL STATES ---"
            grep -F '[K2_CALL_STATE]' /tmp/k2-call-proof.tap || true
            echo "--- ACTIVE COUNT ---"
            grep -F '[K2_CALL_STATE]' /tmp/k2-call-proof.tap | grep -c '"status":"ACTIVE"' || true
            echo "--- VIDEO STATE COUNT ---"
            grep -F '[K2_CALL_STATE]' /tmp/k2-call-proof.tap | grep -Ec '"selfVideo":true|"peerVideo":true' || true
            echo "--- TOTALS ---"
            grep -E '^# (tests|pass|fail|cancelled|skipped|todo|duration_ms) ' /tmp/k2-call-proof.tap || true
            echo "--- FAILURES ---"
            grep -A35 '^not ok ' /tmp/k2-call-proof.tap || true
          } | tee /tmp/k2-call-proof-report.txt
      - name: Upload proof artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: k2-call-signal-browser-proof
          path: |
            /tmp/k2-call-proof.tap
            /tmp/k2-call-proof-report.txt
            /tmp/k2-call-proof-phoenix.log
          if-no-files-found: warn
          retention-days: 7
      - name: Stop Phoenix
        if: always()
        run: |
          if [ -f /tmp/k2-call-proof-phoenix.pid ]; then kill "$(cat /tmp/k2-call-proof-phoenix.pid)" 2>/dev/null || true; fi
