name: Relationship Reconnect Reservation Proof

on:
  pull_request:
    paths:
      - "lib/strangertalks_new/relationship_reconnections.ex"
      - "test/strangertalks_new/relationship_reconnection_pairing_reservation_test.exs"
      - ".github/workflows/relationship-reconnect-reservation-proof.yml"

permissions:
  contents: read

jobs:
  reconnect-reservation:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: strangertalks_local
          POSTGRES_PASSWORD: strangertalks_test
          POSTGRES_DB: strangertalks_new_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U strangertalks_local -d strangertalks_new_test"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      MIX_ENV: test
      STRANGERTALKS_LOCAL_DB_USER: strangertalks_local
      STRANGERTALKS_LOCAL_DB_PASSWORD: strangertalks_test
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Prove exact candidate
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - uses: erlef/setup-beam@v1
        with:
          elixir-version: "1.18.4"
          otp-version: "27.3.4"
      - run: mix local.hex --force && mix local.rebar --force
      - run: mix deps.get
      - name: Focused reconnection reservation and race proof
        run: >-
          mix test
          test/strangertalks_new/relationship_reconnection_pairing_reservation_test.exs
          test/strangertalks_new/relationship_reconnections_test.exs
      - name: Diff hygiene
        run: git diff --check
